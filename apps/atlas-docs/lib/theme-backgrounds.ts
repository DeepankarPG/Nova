/**
 * Page background colors — must stay in sync with `app/globals.css`
 * `:root { --background }` and `.dark { --background }`.
 */
export const THEME_PAGE_BACKGROUND = {
  light: "#f6f8fa",
  dark: "#0b0f14",
} as const;
